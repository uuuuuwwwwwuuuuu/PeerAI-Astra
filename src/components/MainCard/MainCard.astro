---
import './mainCard.scss';

interface IProps {
    title: string
    secondInfo: string
    gifSrc: string
}

const {title, secondInfo, gifSrc} = Astro.props as IProps

---

<article class="main_card_article">
    <div class="left_main_card_elements">
        <h2>{title}</h2>
        <slot name="info" />
        <p>{secondInfo}</p>
    </div>
    <div class="right_main_card_elements">
        <img src={gifSrc} alt="gif" />
    </div>
</article>